import RuntimeConfigClient from "./clients";
export const metadata = {
    title: 'Using Runtime Configuration in Next.js 14',
    description: 'Learn about the Using Runtime Configuration in Next.js 14',
   
  };
const RuntimeConfig = ()=>{
    return (
        <RuntimeConfigClient />
    )
}

export default RuntimeConfig